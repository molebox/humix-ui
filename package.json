{
    "private": true,
    "scripts": {
        "gatsby:dev": "yarn workspace gatsby-example dev",
        "next:dev": "yarn workspace next-example dev",
        "sb": "yarn workspace humix-ui sb",
        "sb:build": "yarn workspace humix-ui build:storybook",
        "humix:build": "yarn workspace humix-ui build",
        "humix:publish": "yarn workspace humix-ui publish:lib"
    },
    "workspaces": [
        "packages/*"
    ]
}